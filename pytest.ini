# pytest.ini - Update your existing file or create new one
[pytest]
DJANGO_SETTINGS_MODULE = config.test_settings
python_files = test_*.py
addopts = 
    --reuse-db
    --create-db
    -v
    --tb=short
    --nomigrations